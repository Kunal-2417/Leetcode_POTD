#include<bits/stdc++.h>
using namespace std;
#define ll long long
class Solution
{
public:
    int compress(vector<char> &chars)
    {
        string ans;
        int n=chars.size();
        
    }
};
int main()
{
    Solution s;
    vector<char> chars = {'a', 'a', 'b', 'b', 'c', 'c', 'c'};
    cout << s.compress(chars) << endl; 

return 0;
}